# Godot 4+ specific ignores
.godot/
.nomedia

# Godot-specific ignores
.import/
export.cfg
export_credentials.cfg
*.tmp

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# Built by tools/update_amlib.sh
addons/amlib/bin/
/build/

# Android build template: keep our changes, not its output or the engine
# libraries (Godot installs those from its export templates)
android/build/build/
android/build/.gradle/
android/build/libs/
